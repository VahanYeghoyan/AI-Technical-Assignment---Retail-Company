# Verified working on Python 3.14.0 (macOS, arm64) on 2026-09-18.
# Python >= 3.13 is required: on 3.12 pip resolves google-adk to 1.14.1,
# a much older API than the 2.x this code is written against.

google-adk==2.9.1
google-genai==2.24.0
google-cloud-bigquery==3.45.2
db-dtypes>=1.4.3
pandas>=2.2
# pandas.DataFrame.to_markdown() needs this. It is an *optional* pandas extra,
# so without it the failure appears at runtime, mid-turn, rather than at import.
tabulate>=0.9
sqlglot==30.18.0

rich>=13.7
prompt_toolkit>=3.0.47
python-dotenv>=1.0
pydantic>=2.12
tenacity>=9.0
PyYAML>=6.0

pytest>=8.3
pytest-asyncio>=0.24
