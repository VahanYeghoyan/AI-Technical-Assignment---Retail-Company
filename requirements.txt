# Verified working on Python 3.14.0 (macOS, arm64) on 2026-09-18.
# Python >= 3.13 recommended: on 3.12, pip resolves google-genai to 1.75.0
# rather than the 2.x this code was written and verified against.
# Python >= 3.11 is a hard floor (datetime.UTC, StrEnum, tomllib-era syntax).

google-genai==2.24.0
google-cloud-bigquery==3.45.2
db-dtypes>=1.4.3
pandas>=2.2
# pandas.DataFrame.to_markdown() needs this. It is an *optional* pandas extra,
# so without it the failure appears at runtime, mid-turn, rather than at import.
tabulate>=0.9
sqlglot==30.18.0

rich>=13.7
prompt_toolkit>=3.0.47
python-dotenv>=1.0
pydantic>=2.12
tenacity>=9.0
PyYAML>=6.0

pytest>=8.3
pytest-asyncio>=0.24

# The optional web UI is NOT installed by this file — the CLI is the required
# interface and should not pull in a web stack. To run it as well:
#   pip install -r requirements-ui.txt && streamlit run streamlit_app.py
