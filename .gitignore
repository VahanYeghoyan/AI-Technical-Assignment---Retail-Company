# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# Secrets — never commit
.env

# Local state: saved reports (var/reports.db) and traces (var/traces/)
var/

# The Google Cloud SDK installer unpacks into the directory it is run from. If
# that is ever this project, it must never be committed (hundreds of MB).
google-cloud-sdk/
google-cloud-cli-*.tar.gz

# macOS
.DS_Store
