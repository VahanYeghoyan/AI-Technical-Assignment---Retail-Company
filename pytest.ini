[pytest]
# Collect only our own tests. Without testpaths, a bare `pytest` walks into any
# directory under the project root — including an unpacked google-cloud-sdk,
# whose vendored third-party test suites produce hundreds of collection errors.
testpaths = tests
norecursedirs = google-cloud-sdk .venv var .git __pycache__
addopts = -q
